package tests;

import dto.AddCarDTO;
import org.testng.annotations.Test;
import utils.RandomUtils;

public class AddNewCarTests extends BaseTest{
/*
    @Test
    public void addNewCarTest() throws InterruptedException {
        sesearchPage.openLoginPage();
        loginPage.login(user);
        searchPage.openMenu();
        searchPage.openMyCarsPage();
        myCarsPage.clickAddNewCar();

        String serNumber = RandomUtils.generateStringDigits(11);
        carsSerNumbers[index] = serNumber;
        index++;
        AddCarDTO addNewCar = AddCarDTO.builder()
                .serialNumber(serNumber)
                .manufacture("qa39")
                .model("corsa")
                .year(1990)
                .fuel("Diesel")
                .seats(2)
                .carClass("ads")
                .pricePerDay(9)
                .about("fff")
                .city("Tel Aviv")
                .build();
        addNewCarPage.addNewCar(addNewCar);
        Thread.sleep(5000);

    }*/
}
